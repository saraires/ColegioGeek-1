import React, { useState, createContext, useEffect } from "react";
import axios from "axios";

export const datos = createContext()
export const datosPriver = (props) => {
    const [id, setId] = useState("")

    useEffect(()=>{
        
    })
}

